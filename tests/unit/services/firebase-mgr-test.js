import { moduleFor, test } from 'ember-qunit';

moduleFor('service:firebase-mgr', 'Unit | Service | firebase mgr', {
  integration: true
});

// Replace this with your real tests.
test('it exists', function(assert) {
  let service = this.subject();
  assert.ok(service);
});
