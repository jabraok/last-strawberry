import { moduleFor, test } from 'ember-qunit';

moduleFor('route:purchase-orders/show', 'Unit | Route | purchase orders/show', {
  integration: true
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
