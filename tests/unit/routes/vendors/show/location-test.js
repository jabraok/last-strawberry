import { moduleFor, test } from 'ember-qunit';

moduleFor('route:vendors/show/location', 'Unit | Route | vendors/show/location', {
  integration: true
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
