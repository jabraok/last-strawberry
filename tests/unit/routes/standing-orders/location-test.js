import { moduleFor, test } from 'ember-qunit';

moduleFor('route:standing-orders/location', 'Unit | Route | standing orders/location', {
  integration: true
});

test('it exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});
